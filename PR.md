# 世界コンピューター将棋選手権 参加PR文書
## 将棋AIエンジン **tiger**

---

## 1. はじめに

**tiger** は、Rust で実装された将棋AIエンジンです。  
本エンジンは、対局時の安定した探索性能と、学習時の高速性・省メモリ性の両立を目標として開発しています。

tiger の特徴は、単に「将棋エンジンを作る」ことにとどまらず、**対局基盤・探索基盤・学習基盤を自作ライブラリ群によって一貫して設計している点**にあります。  
具体的には、以下の自作フレームワークを中心に構成されています。

- **USIAgent**  
  USIプロトコル処理、差し手生成、差し手の並び替えなど、将棋エンジンとして必要な基盤機能を提供するフレームワーク
- **nncombinator**  
  簡潔なコードでニューラルネットワークを構築し、推論・学習を実行できるディープラーニングフレームワーク

さらに tiger では、nncombinator のための**独自特徴量変換層**を実装しており、CPU 推論と GPU 学習の双方で高効率に動作するよう最適化しています。  
探索だけでなく、評価関数の学習部分まで含めて、自作実装による統合設計を進めている点が本エンジンの大きな特徴です。

---

## 2. 開発方針

tiger の開発では、次の3点を重視しています。

### 2.1 一貫した自作基盤による実装
既存の仕組みを部分的に組み合わせるのではなく、  
**プロトコル処理・探索・評価・学習をできるだけ統一的な設計思想で構築すること**を重視しています。

そのため、エンジン本体だけでなく、

- USI対応
- 特徴量処理
- ニューラルネットワーク学習
- 教師局面の読み込み
- データローダ

といった周辺機構も含めて、自作ライブラリを中心に整備しています。

### 2.2 実運用を見据えた高速化
将棋AIでは、探索速度だけでなく、学習パイプライン全体の処理効率も重要です。  
tiger では、**CPU 上の推論速度**と**GPU 上の学習速度・メモリ効率**の両面を意識した実装を行っています。

### 2.3 Rust による安全性と性能の両立
tiger は **Rust** で実装されています。  
Rust は、高い実行性能を維持しながら、所有権システムと型安全性によってバグを抑えやすい言語です。  
将棋AIのように複雑な状態管理と高速処理を両立したい用途において、Rust は非常に相性が良いと考えています。

---

## 3. 自作フレームワーク USIAgent

tiger の対局基盤には、自作フレームワーク **USIAgent** を使用しています。

USIAgent は、将棋エンジン開発において繰り返し必要となる基本機能をまとめたフレームワークであり、特に以下の点を担っています。

- **USIプロトコルの処理の自動化**
- **高速な差し手生成**
- **差し手並び替え器の提供**

### 3.1 USIプロトコル処理の自動化
将棋エンジンを大会やGUI環境で運用する上では、USIプロトコルへの適切な対応が不可欠です。  
USIAgent では、エンジン開発者が探索本体や評価関数の実装に集中できるよう、**USIプロトコル処理をフレームワーク側で吸収・自動化**しています。

これにより、

- エンジンとしての基本動作を安定して実装しやすい
- プロトコル周辺の定型処理を何度も書かずに済む
- 探索部や評価部の改良サイクルを回しやすい

といった利点があります。

### 3.2 高速な差し手生成
探索性能の基礎となるのが差し手生成です。  
USIAgent は、探索の土台として十分な性能を持つ**高速な差し手生成機構**を備えています。  
将棋AIにおいては、1手ごとの合法手列挙や局面更新が全体性能に直結するため、この部分をフレームワークとして整備していることは、エンジン全体の安定性と速度に大きく寄与します。

### 3.3 差し手並び替え器
探索の効率を高めるためには、良い手をなるべく早く探索することが重要です。  
USIAgent は**差し手並び替え器**を備えており、探索アルゴリズムと組み合わせることで枝刈り効率の改善に貢献します。

将棋エンジンの強さは、探索アルゴリズム単体ではなく、

- 手生成
- 手の順序付け
- 評価
- 局面管理

の総合力で決まります。  
USIAgent は、そのうち基盤部分をしっかり支える役割を担っています。

---

## 4. ディープラーニングフレームワーク nncombinator

tiger の学習基盤には、自作ディープラーニングフレームワーク **nncombinator** を使用しています。

nncombinator は、**簡潔なコードでニューラルネットワークを構築し、推論・学習を実行できる**ことを目指して設計されたフレームワークです。  
レイヤを積み上げていく形でネットワークを記述でき、実験や改良を柔軟に進めやすい構造になっています。

以下は nncombinator によるネットワーク構築コードの一例です。

```rust
let mut rnd = prelude::thread_rng();
let rnd_base = Rc::new(RefCell::new(XorShiftRng::from_seed(rnd.gen())));

let n1 = Normal::<f32>::new(0.0, (2f32/(28f32*28f32)).sqrt()).unwrap();
let n2 = Normal::<f32>::new(0.0, (2f32/512f32).sqrt()).unwrap();
let n3 = Normal::<f32>::new(0.0, 1f32/(256f32).sqrt()).unwrap();

let memory_pool = &SHARED_MEMORY_POOL.clone();

let device = DeviceGpu::new(memory_pool).unwrap();

let net:InputLayer<f32,Arr<f32,{ 28*28 }>,_,_> = InputLayer::new(&device);

let rnd = rnd_base.clone();

let optimizer_builder = AdamBuilder::new(&device).lr(0.005).weight_decay(0.0001);

let mut net = net.add_layer(|l| {
    let rnd = rnd.clone();
    LinearLayerBuilder::<{ 28*28 },512>::new().build(l,&device,
        move || n1.sample(&mut rnd.borrow_mut().deref_mut()), || 0.,
        &optimizer_builder
    ).unwrap()
}).add_layer(|l| {
    ActivationLayer::new(l,ReLu::new(&device),&device)
}).add_layer(|l| {
    let rnd = rnd.clone();
    LinearLayerBuilder::<512,256>::new().build(l,&device,
        move || n2.sample(&mut rnd.borrow_mut().deref_mut()), || 0.,
        &optimizer_builder
    ).unwrap()
}).add_layer(|l| {
    ActivationLayer::new(l,ReLu::new(&device),&device)
}).add_layer(|l| {
    let rnd = rnd.clone();
    LinearLayerBuilder::<256,10>::new().build(l,&device,
    move || n3.sample(&mut rnd.borrow_mut().deref_mut()), || 0.,
        &optimizer_builder
    ).unwrap()
}).add_layer(|l| {
    ActivationLayer::new(l,SoftMax::new(&device),&device)
}).add_layer(|l| {
    LinearOutputLayer::new(l,&device).unwrap()
});
```


このサンプルからも分かる通り、nncombinator では、

- 入力層の定義
- 線形層の追加
- 活性化関数の追加
- 出力層の定義
- 最適化手法の設定

といった処理を、比較的見通しの良い形で記述できます。  
将棋AIの評価関数学習では、特徴量設計や層構成の試行錯誤が多く発生するため、**簡潔で再構成しやすい記述性**は大きな利点です。

---

## 5. tiger 独自の特徴量変換層

tiger の中核的な特徴の一つが、**nncombinator のインターフェースを実装した独自の特徴量変換層**です。

将棋の評価関数では、局面から得られる疎な特徴量を効率よく扱うことが重要です。  
tiger ではこの特性を踏まえ、入力の性質に合わせて CPU 推論と GPU 学習のそれぞれに適した実装を行っています。

---

## 6. CPU推論時の高速化

CPU 実行時の推論では、**portable SIMD** を用いて特徴量変換層を高速化しています。

将棋の特徴量は一般に非常に疎であり、各局面でアクティブになる入力は全体のごく一部です。  
そこで tiger では、特徴量変換の順伝播において、**アクティブな入力位置のインデックスのリスト**を受け取り、その位置に対応する重みだけを加算していく方式を採用しています。

### 6.1 入力が 0/1 であることの利用
この特徴量変換層では、入力値は本質的に **0 か 1** の二値です。  
したがって、通常の全結合層のように

- 入力値 × 重み

という一般的な乗算を毎回行う必要がありません。  
アクティブな特徴については、**対応する重み行を単純に加算するだけでよい**ため、計算量を削減できます。

### 6.2 portable SIMD によるベクトル化
さらに、出力側の加算処理には portable SIMD を用いることで、複数要素をまとめて処理しています。  
これにより、CPU 上でも疎な特徴量に対する変換を効率よく実行できます。

将棋エンジンにおいては、探索中に評価関数が非常に多数回呼ばれるため、このような推論高速化はそのまま探索性能の向上につながります。  
言い換えると、**1回の評価を少しでも軽くする工夫の積み重ねが、最終的な指し手の質に返ってくる**という考え方です。

---

## 7. GPU学習時の高速化と省メモリ化

tiger では、GPU 学習時にも独自の工夫を行っています。  
特徴量変換層について、**独自の CUDA カーネル**を実装し、順伝播および重み更新量計算を高速・省メモリに処理しています。

---

## 8. 順伝播時の工夫

順伝播では、入力として**アクティブになる入力位置のインデックス列**をカーネルに渡します。  
ここでも入力は 0/1 の二値であるため、重み付き和の計算は「アクティブな行の加算」に置き換えられます。

### 8.1 乗算を使わない計算
通常の密な行列演算では、各入力要素に対して乗算と加算が必要です。  
しかし tiger の特徴量変換では、入力が 0 か 1 しか取らないため、

- 0 のときは何もしない
- 1 のときは対応する重みを足す

という形に簡約できます。  
これにより、**不要な乗算を避ける**ことができます。

### 8.2 warp shuffle リダクション
GPU 上では、アクティブな特徴に対応する加算結果を効率よくまとめるために、  
**warp shuffle リダクション**を利用して総和を計算しています。

この方法により、共有メモリ使用量や同期コストを抑えつつ、高速に集約処理を行えます。  
疎な入力に対して必要な演算だけを重点的に処理する設計になっており、将棋評価関数に適した実装です。

---

## 9. 重み更新量の計算時の工夫

学習時のもう一つの要点が、**重み更新量の計算の効率化**です。

tiger では、重み更新量の計算時に、入力をそのまま密な配列として送るのではなく、  
**ビットベクタとして生成して GPU に転送**します。

### 9.1 ビットベクタ転送による省メモリ化
入力が 0/1 の二値である以上、1要素ごとに通常の浮動小数点数を転送するのは無駄が大きくなります。  
そこで tiger では、入力をビット表現に圧縮して転送することで、**GPU メモリ使用量と転送量を大幅に削減**しています。

この工夫には次の利点があります。

- GPU メモリ消費を抑えられる
- より大きなバッチサイズを扱いやすくなる
- 転送量削減により学習全体のスループット向上が期待できる

### 9.2 カーネル内での逐次部分行列展開
GPU 側では、転送されたビットベクタをカーネル内で**逐次的に部分行列へ展開**しながら処理します。  
密な入力行列をあらかじめ GPU メモリ上に大きく展開して保持するのではなく、必要な形へ変換しながら演算することで、メモリ効率を高めています。

### 9.3 WMMA API の利用
重み更新量の計算には、**WMMA API** を用いています。  
これにより、GPU の行列演算機能を活用しつつ、疎な二値入力という問題設定に合わせた形で計算を進めています。

結果として、

- **メモリ節約**
- **バッチサイズ拡大**
- **計算高速化**

を同時に狙った設計になっています。  
将棋AIの学習では大量の教師局面を扱うため、こうした改善は実験速度そのものに直結します。

---

## 10. 学習周辺基盤の自作実装

tiger では、ニューラルネットワーク本体だけでなく、学習に必要な周辺基盤についても自前のライブラリを用いて実装しています。

具体的には、以下のような要素です。

- **教師局面ファイルを読み取るための機能**
- **学習時に用いるデータローダ**

将棋AIの開発では、評価関数の構造そのものだけでなく、

- どのように教師データを用意するか
- どのように読み込むか
- どのようにバッチ化するか
- どのように継続的に実験を回すか

といった周辺の仕組みが非常に重要です。  
この部分を自作基盤として整備することで、tiger では学習実験の自由度と再現性を高めています。

また、学習パイプライン全体を自分たちで把握できることは、性能改善のボトルネックを見つけやすいという点でも有利です。  
「どこで遅いのか」「どこでメモリを使っているのか」が見えやすいため、実装改善を段階的に進めやすくなります。

---

## 11. Rustで実装する意義

tiger は全体として **Rust** を用いて実装されています。  
Rust を採用した理由は、単に新しい言語だからではなく、将棋AIのようなシステムに適した特性を持っているためです。

### 11.1 安全性
将棋エンジンは、探索、局面更新、ハッシュ管理、学習用データ処理など、多数の要素が絡み合う大規模ソフトウェアです。  
Rust の所有権・借用の仕組みは、こうした複雑な実装で発生しがちな不正な参照やメモリ破壊を抑える助けになります。

### 11.2 性能
一方で、Rust はシステムプログラミング言語として高い実行性能を備えており、  
CPU 上の探索処理や特徴量計算にも十分対応できます。  
将棋AIでは、抽象化のしやすさと低レベル最適化の両立が必要ですが、Rust はそのバランスに優れています。

### 11.3 保守性
型システムが豊かで、意図をコードに表現しやすいため、長期的な改良にも向いています。  
大会参加を見据えたエンジン開発では、短期的な速度だけでなく、**継続的に改善し続けられる設計**が重要です。  
tiger では、Rust の特性を活かしてその基盤作りを進めています。

---

## 12. tiger の目指す方向性

tiger は、単なる一時的な実験エンジンではなく、  
**対局・学習・改良を一体として発展させていく将棋AI基盤**を目指しています。

本エンジンの方向性は、以下のように整理できます。

- **USIAgent による堅牢な対局基盤**
- **nncombinator による柔軟な学習基盤**
- **将棋向けに最適化した独自特徴量変換**
- **CPU 推論高速化**
- **GPU 学習高速化・省メモリ化**
- **周辺ツールも含めた一貫した自作実装**
- **Rust による安全性と性能の両立**

将棋AIは、探索だけ強ければよいわけでも、学習だけ速ければよいわけでもありません。  
実際には、

- 強い評価関数を作る学習基盤
- それを対局時に活かす探索基盤
- 両者をつなぐデータ処理基盤

のすべてが揃って初めて、継続的に強くなれるエンジンになります。  
tiger は、その全体像を意識して開発を進めています。

---

## 13. おわりに

tiger は、**USIAgent** と **nncombinator** という自作フレームワークを基盤に、Rust で実装された将棋AIエンジンです。  
USI対応、差し手生成、手の並び替え、ニューラルネットワーク構築、特徴量変換、GPU 学習最適化、教師局面処理、データローダに至るまで、可能な限り一貫した設計で開発しています。

特に、将棋向けの疎な二値特徴量を前提とした独自特徴量変換層により、

- CPU では portable SIMD を活用した高速推論
- GPU では独自 CUDA カーネルによる高速・省メモリ学習

を実現している点が大きな特徴です。

今後も、探索性能・評価性能・学習効率のすべてを高めながら、  
将棋AIとしての完成度を上げていきたいと考えています。

世界コンピューター将棋選手権では、  
**自作基盤を軸に育ててきた Rust 製将棋AI “tiger”** として参加します。  
どうぞよろしくお願いいたします。

---

## 付記：要約

**tiger の主な特徴**

- Rust 製の将棋AIエンジン
- 自作フレームワーク **USIAgent** を利用
    - USIプロトコル処理の自動化
    - 高速な差し手生成
    - 差し手並び替え器
- 自作ディープラーニングフレームワーク **nncombinator** を利用
    - 簡潔なコードでネットワーク構築
    - 推論・学習に対応
- nncombinator インターフェースを実装した独自特徴量変換層
- CPU 推論では portable SIMD を活用
- GPU 学習では独自 CUDA カーネルを活用
    - 順伝播でインデックス列入力＋warp shuffle リダクション
    - 重み更新量計算でビットベクタ転送＋WMMA API 利用
- 教師局面ファイル読込やデータローダも自前実装
